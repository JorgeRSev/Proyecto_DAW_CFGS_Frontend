services:
  - type: web
    name: pelupatas-frontend
    runtime: static

    buildCommand: cd pelupatas/frontend && npm install && npm run build -- --configuration production

    staticPublishPath: pelupatas/frontend/dist/frontend/browser

    routes:
      - type: rewrite
        source: /*
        destination: /index.html
